import { ArrayInfexFilterPipe } from './array-infex-filter.pipe';

describe('ArrayInfexFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayInfexFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
