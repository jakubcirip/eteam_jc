import { EmailTemplatePipe } from './email-template.pipe';

describe('EmailTemplatePipe', () => {
  it('create an instance', () => {
    const pipe = new EmailTemplatePipe();
    expect(pipe).toBeTruthy();
  });
});
